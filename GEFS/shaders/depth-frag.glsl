#version 330 core

// Ouput data
//layout(location = 0) out float fragmentdepth;
//out vec4 outColor;

void main(){
    // Not really needed, OpenGL does it anyway
    //gl_FragDepth = 1; //gl_FragCoord.z;
    //outColor = vec4(0.0,0.5,1,1);
}